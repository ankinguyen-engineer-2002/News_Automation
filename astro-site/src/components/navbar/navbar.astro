---
import Container from "@/components/container.astro";
import Link from "@/components/ui/link.astro";
import Dropdown from "./dropdown.astro";
import { Astronav, MenuItems, MenuIcon } from "astro-navbar";

const base = import.meta.env.BASE_URL;

const topics = [
  { title: "Microsoft", slug: "microsoft", color: "topic-microsoft" },
  { title: "Data Platform", slug: "data-platform", color: "topic-data-platform" },
  { title: "Analytics", slug: "analytics-engineering", color: "topic-analytics-engineering" },
  { title: "AI / LLM", slug: "ai-agents", color: "topic-ai-agents" },
  { title: "Automation", slug: "automation", color: "topic-automation" },
  { title: "GitHub", slug: "github", color: "topic-github" },
];

const menuitems = [
  {
    title: "Topics",
    path: "#",
    children: topics.map(t => ({
      title: t.title,
      path: `${base}/topics/${t.slug}`,
      color: t.color,
    })),
  },
  {
    title: "Archive",
    path: `${base}/archive`,
  },
  {
    title: "About",
    path: `${base}/about`,
  },
];
---

<Container>
  <header class="flex flex-col lg:flex-row justify-between items-center my-5">
    <Astronav>
      <div class="flex w-full lg:w-auto items-center justify-between">
        <a href={base || "/"} class="text-lg flex items-center gap-2">
          <span class="text-xl">📰</span>
          <span class="font-bold text-slate-800 dark:text-white">Daily</span>
          <span class="text-slate-500 dark:text-slate-400">Intel</span>
        </a>
        <div class="block lg:hidden">
          <MenuIcon class="w-4 h-4 text-gray-800 dark:text-gray-200" />
        </div>
      </div>
      <MenuItems class="hidden w-full lg:w-auto mt-2 lg:flex lg:mt-0">
        <ul class="flex flex-col lg:flex-row lg:gap-3">
          {
            menuitems.map((item, index) => (
              <>
                {item.children && (
                  <li class="relative">
                    <menu class="astronav-dropdown group" aria-expanded="false">
                      <button class="flex items-center gap-1 w-full lg:w-auto lg:px-3 py-2 text-gray-600 dark:text-gray-300 hover:text-gray-900 dark:hover:text-white">
                        <span>{item.title}</span>
                        <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="3" stroke="currentColor" class="w-3 h-3 mt-0.5 group-open:rotate-180 transition-transform">
                          <path stroke-linecap="round" stroke-linejoin="round" d="M19.5 8.25l-7.5 7.5-7.5-7.5"></path>
                        </svg>
                      </button>
                      <div class="astronav-dropdown dropdown-toggle hidden" aria-expanded="false">
                        <div class="lg:absolute w-full lg:w-48 z-10 lg:left-0 origin-top-left">
                          <div class="px-3 lg:py-2 lg:bg-white dark:lg:bg-slate-800 lg:rounded-md lg:shadow-lg lg:border dark:lg:border-slate-700 flex flex-col">
                            {item.children.map(child => (
                              <a href={child.path} class="py-2 flex items-center gap-2 text-gray-600 dark:text-gray-300 hover:text-gray-900 dark:hover:text-white transition">
                                <span class={`topic-indicator ${child.color}`}></span>
                                <span>{child.title}</span>
                              </a>
                            ))}
                          </div>
                        </div>
                      </div>
                    </menu>
                  </li>
                )}

                {!item.children && (
                  <li>
                    <a
                      href={item.path}
                      class="flex lg:px-3 py-2 items-center text-gray-600 dark:text-gray-300 hover:text-gray-900 dark:hover:text-white transition">
                      <span>{item.title}</span>
                    </a>
                  </li>
                )}
              </>
            ))
          }
        </ul>
      </MenuItems>
    </Astronav>
    
    <!-- Theme + Language Toggle -->
    <div class="hidden lg:flex items-center gap-3">
      <!-- Theme Toggle -->
      <button 
        id="theme-toggle"
        class="p-2 rounded-lg bg-slate-100 dark:bg-slate-800 hover:bg-slate-200 dark:hover:bg-slate-700 transition"
        aria-label="Toggle theme"
      >
        <svg id="sun-icon" class="w-5 h-5 text-amber-500 hidden dark:block" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 3v1m0 16v1m9-9h-1M4 12H3m15.364 6.364l-.707-.707M6.343 6.343l-.707-.707m12.728 0l-.707.707M6.343 17.657l-.707.707M16 12a4 4 0 11-8 0 4 4 0 018 0z"></path>
        </svg>
        <svg id="moon-icon" class="w-5 h-5 text-slate-600 dark:hidden" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M20.354 15.354A9 9 0 018.646 3.646 9.003 9.003 0 0012 21a9.003 9.003 0 008.354-5.646z"></path>
        </svg>
      </button>
      
      <!-- Language Toggle -->
      <button 
        id="lang-toggle"
        class="px-3 py-1.5 text-sm font-medium bg-slate-100 dark:bg-slate-800 hover:bg-slate-200 dark:hover:bg-slate-700 rounded-lg transition flex items-center gap-1.5"
      >
        <span id="lang-label">EN</span>
        <svg class="w-3 h-3" fill="none" viewBox="0 0 24 24" stroke="currentColor">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 9l4-4 4 4m0 6l-4 4-4-4" />
        </svg>
      </button>
    </div>
  </header>
</Container>

<script>
  // Theme toggle - runs on every page load
  function initTheme() {
    const themeToggle = document.getElementById('theme-toggle');
    const sunIcon = document.getElementById('sun-icon');
    const moonIcon = document.getElementById('moon-icon');
    
    function updateIcons() {
      const isDark = document.documentElement.classList.contains('dark');
      if (sunIcon && moonIcon) {
        sunIcon.style.display = isDark ? 'block' : 'none';
        moonIcon.style.display = isDark ? 'none' : 'block';
      }
    }
    
    // Initial icon state
    updateIcons();
    
    themeToggle?.addEventListener('click', () => {
      const isDark = document.documentElement.classList.contains('dark');
      
      if (isDark) {
        document.documentElement.classList.remove('dark');
        localStorage.setItem('theme', 'light');
      } else {
        document.documentElement.classList.add('dark');
        localStorage.setItem('theme', 'dark');
      }
      
      updateIcons();
    });
  }

  // Language toggle
  function initLang() {
    const langToggle = document.getElementById('lang-toggle');
    const langLabel = document.getElementById('lang-label');
    
    const savedLang = localStorage.getItem('lang') || 'en';
    document.documentElement.setAttribute('data-lang', savedLang);
    if (langLabel) langLabel.textContent = savedLang.toUpperCase();
    
    langToggle?.addEventListener('click', () => {
      const currentLang = document.documentElement.getAttribute('data-lang') || 'en';
      const newLang = currentLang === 'en' ? 'vi' : 'en';
      document.documentElement.setAttribute('data-lang', newLang);
      localStorage.setItem('lang', newLang);
      if (langLabel) langLabel.textContent = newLang.toUpperCase();
    });
  }

  // Initialize on DOM ready
  document.addEventListener('DOMContentLoaded', () => {
    initTheme();
    initLang();
  });
  
  // Also run immediately for Astro's view transitions
  initTheme();
  initLang();
</script>

